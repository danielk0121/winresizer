# 미구현 기능 및 고도화 작업 목록 (TODO)

## 1. 윈도우 제어 로직 (P1)
- [x] **1/4 분할 모드 (Corner Snapping):** 좌상, 우상, 좌하, 우하 1/4 분할 좌표 계산 로직 추가 (`coordinate_calculator.py`)
- [x] **상/하 1/2 분할:** 가로 방향 분할 외에 세로 방향 1/2 분할 추가
- [x] **2/3 분할 모드:** 좌측 2/3, 우측 2/3 분할 로직 추가
- [x] **윈도우 상태 복구 (Restore):** 조절 전 상태를 저장하고 복구하는 기능 완성 (`window_manager.py`)

## 2. 시스템 고도화 및 UX (P2)
- [x] **윈도우 간 간격 (Gaps) UI 설정:** 현재 코드에는 하드코딩된 gap이 있으나, 사용자가 GUI에서 조절할 수 있어야 함
- [ ] **윈도우 스냅 애니메이션:** 창 이동 시 부드러운 애니메이션 효과 적용 (`pyobjc` 활용 방안 연구)
- [x] **멀티 모니터 이동 강화:** `다음_디스플레이` 외에 `이전_디스플레이` 명령 및 단축키 추가

## 3. 지능형 기능 확장 (P3)
- [ ] **앱별 자동 레이아웃 (Auto-Layout Profiles):** 특정 앱 실행 시 지정된 위치로 자동 배치하는 로직 및 설정 추가
- [x] **윈도우 제외 리스트 (Ignore Apps):** 단축키 충돌 방지를 위한 특정 앱 제외 설정 기능
- [ ] **외부 명령 연동 (URL Scheme):** `winresizer://` 프로토콜 등록 및 외부 호출 처리

## 4. GUI 개선
- [x] **스크롤 뷰 도입:** 설정 항목이 많어짐에 따라 GUI에 스크롤 기능 추가
- [ ] **다크 모드 최적화:** macOS 시스템 테마 변경 시 실시간 반영 로직 확인

## 5. 배포 및 설치 (P4)
- [ ] **독립 실행형 앱 번들링:** PyInstaller 또는 py2app을 이용한 `.app` 생성 스크립트 작성
- [ ] **전용 앱 아이콘 제작:** macOS 스타일의 `.icns` 아이콘 적용
- [ ] **DMG 패키징:** `create-dmg` 등을 이용한 설치용 디스크 이미지 생성 및 배포 자동화

